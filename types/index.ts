export interface SearchResult {
  id: number;
  title: string;
  description: string;
  category: string;
}

export interface ApiSearchResult {
  id: number;
  name: string;
  desc: string;
  type: string;
}
